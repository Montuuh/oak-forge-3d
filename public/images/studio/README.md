# Referencia de escena (prompt v7)

Coloca aquí la foto del estudio de marca:

- `default.jpg` (o `.webp`) — escena por defecto (mesa roble + pared gotelé)
- Tras subir otra desde admin: `custom.webp` (tiene prioridad)

Sin `default.*`, la generación v7 pedirá subir la referencia de escena.

Prompt sugerido para generar la escena fuera de la app: ver `docs/STUDIO_SCENE_PROMPT.md`.
